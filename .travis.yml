language: node_js
node_js:
  - 'lts/*'

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.13.0
  - export PATH="$HOME/.yarn/bin:$PATH"

after_success:
  - npm i -g codecov
  - node upload-coverage.js
